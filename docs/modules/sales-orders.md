# Sales Orders Module

## Overview

The Sales Orders module handles customer order entry, editing, approval workflows, and lifecycle management. It is the starting point of the entire ERP flow.

## Tables

| Table | Purpose |
|-------|---------|
| `sales_orders` | Order header (SO number, client, dates, transport, billing) |
| `sales_order_lines` | Line items (product, qty, rate, GST, approvals) |
| `sales_order_rollup_cache` | Cached rollup of approval/WO statuses |
| `so_counters` | SO number sequence by financial year and prefix |

## SO Number Format

SO numbers are generated by `generateSalesOrderNumber(prefix, orderDate)`:

- Format: `{PREFIX}-{FY}/{SEQ}` (e.g., `SL-2526/0001`, `M-2526/0042`)
- `PREFIX`: `SL` for label/sticker orders, `M` for trading items
- `FY`: Financial year string (e.g., `2526` for April 2025 - March 2026)
- `SEQ`: Auto-incrementing sequence from `so_counters` table, zero-padded to 4 digits

The `getFinancialYear(dt)` function returns the FY string. April is the start of the Indian financial year.

## Key Functions (Code.gs)

| Function | Purpose |
|----------|---------|
| `saveOrderWithKey(payload, idemKey, apiKey, username)` | Create a new SO with idempotency protection |
| `updateSalesOrder(soId, payload)` | Update an existing SO header and lines |
| `listSalesOrders(options)` | List SOs with filtering, pagination, search |
| `loadSalesOrder(soNumber)` | Load a complete SO with lines for editing |
| `getSalesOrderLines(soId)` | Fetch all lines for an SO |
| `getSalesOrderPrintData(soNumber, token)` | Get SO data formatted for printing |
| `setSalesOrderLifecycleStatus(soNumber, nextStatus, token)` | Change SO status (OPEN/HOLD/CANCELLED) |
| `refreshWorkOrderStatuses(soNumbers)` | Sync WO status back to SO header |

## Order Creation Flow

1. User fills order form in the web app (page: `order`)
2. `saveOrderWithKey()` is called with an idempotency key to prevent duplicates
3. System generates SO number via `generateSalesOrderNumber()`
4. Header inserted into `sales_orders`
5. Lines inserted into `sales_order_lines`
6. `createArtworksFromSoLines(soId)` auto-creates artwork records for each line
7. Rollup cache updated by trigger `trg_sales_order_rollup_from_sales_orders`

## Sales Order Header Fields

| Field | Description |
|-------|-------------|
| `so_number` | Unique identifier (e.g., SL-2526/0001) |
| `order_prefix` | `SL` or `M` |
| `client_code` | FK to clients.client_code |
| `so_date` | Order date |
| `po_number` / `po_date` | Customer's purchase order reference |
| `sales_rep` | Sales representative name |
| `sales_type` | e.g., Regular B2B, SEZ, Export Sales, B2C |
| `currency` | INR or USD |
| `tax_mode` | Tax calculation mode |
| `status` | OPEN, HOLD, CANCELLED |
| `wo_status` | PENDING or CREATED (derived from work_order_jobs) |
| `mode_of_transport` | Transport mode |
| `transport_preference` / `transport_payment` | Transport details (To Pay / Paid) |
| `billing_remarks` | Remarks for billing department |

## Sales Order Line Fields

| Field | Description |
|-------|-------------|
| `product_code` / `product_name` | Product reference (FK to items.item_code) |
| `category` | Product category (e.g., Stickers, Labels, Corrugated Box) |
| `qty` / `unit` / `rate` | Quantity, unit of measure, unit price |
| `disc_pct` / `disc_amount` | Discount percentage and amount |
| `gst_pct` / `hsn_group` | GST rate and HSN code group |
| `cgst` / `sgst` / `igst` | Calculated GST split (CGST+SGST for intra-state, IGST for inter-state) |
| `job_type` | New, Old, Old-Revised, Repeat, Reprint |
| `job_reference` | e.g., Approved Dummy, Approved Ferrow, As per Old Sample |
| `job_priority` | Normal, Low, Medium, High |
| `expected_delivery` / `final_delivery` | Delivery dates |
| `accounts_status` | PENDING / APPROVED / REJECTED / HOLD |
| `business_status` | PENDING / APPROVED / REJECTED / HOLD |
| `division` / `quote_no` / `pm_code` | Additional tracking fields |

## Approval Workflow

Sales order lines require two independent approvals before they can proceed to work order creation:

1. **Accounts Approval** (`accounts_status`) - Finance team verifies pricing, credit terms
2. **Business Approval** (`business_status`) - Management approves the order

Functions:
- `updateSalesOrderLineApproval(payload)` - Update single line approval
- `bulkUpdateSalesOrderApproval(payload)` - Bulk approve/reject multiple lines

RBAC permissions control who can approve:
- `can_approve_accounts` on `SALES_ORDER_APPROVAL` module
- `can_approve_business` on `SALES_ORDER_APPROVAL` module

## SO Listing and Rollup Cache

The `sales_order_rollup_cache` table is maintained by three triggers:
- `trg_sales_order_rollup_from_lines` - When SO lines change
- `trg_sales_order_rollup_from_sales_orders` - When SO header changes
- `trg_sales_order_rollup_from_work_order_jobs` - When WO jobs change

This cache provides:
- `accounts_status` / `business_status` - Rolled up from all lines (PENDING if any pending, APPROVED if all approved)
- `wo_status` - PENDING or CREATED based on work_order_jobs existence
- `line_count` / `total_qty` - Summary counts
- `can_edit` / `can_hold` / `can_cancel` - UI state flags

The `v_sales_orders_list_fast` view joins `sales_orders` with `clients` and `sales_order_rollup_cache` for the listing page.

## SO Lifecycle

```
OPEN ──> HOLD ──> OPEN (reopen)
  |
  └──> CANCELLED (terminal, blocks all downstream)
```

Cancelled SOs are excluded from WO candidate views and billing.

## Views

| View | Purpose |
|------|---------|
| `v_sales_orders_list_fast` | Fast listing with rollup cache |
| `v_sales_orders_dashboard` | Dashboard view with approval/WO status |
| `sales_orders_list` | Legacy listing with line-1 details |
| `v_sales_order_line_details` | Line details with computed fields |
| `v_sales_order_lines_for_approval` | Lines for the approval screen |

## Integration with Other Modules

- **Artwork**: Auto-created when SO lines are saved (`createArtworksFromSoLines`)
- **Work Orders**: SO lines become WO candidates after approval (`v_workorder_candidates`)
- **Billing**: SO lines are referenced by invoice_lines for billing
- **Reports**: `v_report_planning_lines` tracks SO lines through the full lifecycle
